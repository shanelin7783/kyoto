import type { HighlightSegment } from './data/types'

/** Display labels for brochure `segment` (Trad. Chinese). */
export const SEGMENT_LABELS: Record<HighlightSegment, string> = {
  morning: '上午',
  afternoon: '下午',
  evening: '晚上',
  breakfast: '早餐',
  lunch: '中餐',
  dinner: '晚餐'
}
